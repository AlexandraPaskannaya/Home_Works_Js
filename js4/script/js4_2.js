let name = "Василий";
        function hello2() {
            if (name !== "undefined") {
               alert ("Привет, " + name);
            }
            else alert("Привет, гость");
        }
hello2();